export enum statusEnum {
    Progressing,
    Expired,
    Succeeded,
    Failed, Paused
  }

export type componentStyle = {
    iconName : string
    iconColor : string
    smallLabel : string
  }
  